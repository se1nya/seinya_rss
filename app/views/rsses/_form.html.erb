<%= form_for(@rss) do |f| %>
  <% if @rss.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rss.errors.count, "error") %> prohibited this rss from being saved:</h2>

      <ul>
      <% @rss.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
